pull_request_rules:
  - name: BB fork head cleanup identity boundary
    conditions:
      - merged
      - title = BB fork head cleanup canary
    actions:
      delete_head_branch:
        force: false
