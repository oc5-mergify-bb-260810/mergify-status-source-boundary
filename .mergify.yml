pull_request_rules:
  - name: BB fork update repository identity
    conditions:
      - title = BB fork update canary
      - "#commits-behind > 0"
    actions:
      update:
  - name: BB fork rebase repository identity
    conditions:
      - title = BB fork rebase collision canary 20260810
    actions:
      rebase:
